<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Choose Your Plan | Rally AI</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    .pricing-card { border-radius: 15px; transition: 0.3s; border: none; overflow: hidden; height: 100%; }
    .pricing-card:hover { transform: translateY(-10px); box-shadow: 0 15px 35px rgba(0,0,0,0.1); }
    .plan-price { font-size: 2.5rem; font-weight: bold; }
    .feature-list { list-style: none; padding: 0; }
    .feature-list li { margin-bottom: 10px; color: #555; }
    .feature-list i { color: #198754; margin-right: 10px; }
  </style>
</head>
<body class="bg-light">
  <%- include('partials/navbar.ejs') %>

  <div class="container py-5">
    <div class="text-center mb-5">
      <h1 class="fw-bold">Ready to Supercharge Your Content?</h1>
      <p class="text-muted">Choose the membership plan that fits your production needs.</p>
    </div>

    <div class="row row-cols-1 row-cols-md-3 g-4">
      <% if (plans && plans.length > 0) { %>
        <% plans.forEach(plan => { %>
          <div class="col">
            <div class="card pricing-card shadow-sm">
              <div class="card-body p-4 text-center">
                <h4 class="fw-bold"><%= plan.planName %></h4>
                <div class="plan-price my-3">$<%= plan.price %> <small class="text-muted fs-6">/month</small></div>
                
                <hr>
                
                <ul class="feature-list text-start my-4">
                  <li><i class="fas fa-check-circle"></i> <strong><%= plan.aiGenerationLimit %></strong> AI Generations</li>
                  <li><i class="fas fa-check-circle"></i> <strong><%= plan.businessLimit %></strong> Business Projects</li>
                  <li><i class="fas fa-check-circle"></i> <%= plan.templateAccess %> Access</li>
                  <li><i class="fas fa-check-circle"></i> Valid for 30 days</li>
                </ul>

                <form action="/subscribe" method="POST">
                  <input type="hidden" name="planId" value="<%= plan.planId %>">
                  <button type="submit" class="btn <%= plan.planName === 'Personal' ? 'btn-primary' : 'btn-outline-dark' %> rounded-pill w-100 fw-bold">
                    Select <%= plan.planName %>
                  </button>
                </form>
              </div>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <div class="col-12 text-center">
          <div class="alert alert-info">No subscription plans available at the moment.</div>
        </div>
      <% } %>
    </div>
  </div>

  <div class="text-center mb-5"><h2 class="fw-bold">Flexible Membership Plans</h2></div>
    <div class="row row-cols-1 row-cols-md-3 g-4">
      
      <div class="col">
        <div class="card h-100 pricing-card shadow-sm">
          <div class="card-body p-4 text-center">
            <h4>Pay-As-You-Go</h4>
            <h2>$0.75 <small>/clip</small></h2>
            <a href="/subscriptions" class="btn btn-outline-dark rounded-pill mt-3">Get Started</a>
          </div>
        </div>
      </div>

      <div class="col">
        <div class="card h-100 pricing-card border-primary shadow">
          <div class="card-body p-4 text-center">
            <div class="badge bg-primary mb-2">Popular</div>
            <h4>Personal</h4>
            <h2>$25 <small>/month</small></h2>
            <p>60 Credits</p>
            <a href="/subscriptions" class="btn btn-primary rounded-pill mt-3">Join Personal</a>
          </div>
        </div>
      </div>

      <div class="col">
        <div class="card h-100 pricing-card shadow-sm">
          <div class="card-body p-4 text-center">
            <h4>Production</h4>
            <h2>$120 <small>/month</small></h2>
            <p>400 Credits</p>
            <a href="/subscriptions" class="btn btn-dark rounded-pill mt-3">Go Production</a>
          </div>
        </div>
      </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>