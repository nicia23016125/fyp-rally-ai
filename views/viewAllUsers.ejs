<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Rally AI</title>

  <style>
    /* Page Styling */
    body {
      background-image: url('/images/k.jpg'); /* Background image */
      background-size: cover;
      background-position: center;
      font-family: 'Poppins', sans-serif;
      color: #333;
    }

    .container {
      background: rgba(255, 255, 255, 0.9);
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      margin-top: 40px;
      backdrop-filter: blur(10px);
      text-align: center;
    }

    h2 {
      color: #0b3d91; /* Dark blue */
      font-weight: bold;
      margin-bottom: 20px;
    }

    /* Error Message */
    .error-box {
      background: rgba(224, 243, 255, 0.95);
      padding: 20px;
      border-radius: 10px;
      display: inline-block;
      margin-top: 20px;
      font-size: 20px;
      font-weight: bold;
      color: #d9534f;
      border: 2px solid #d9534f;
    }

    /* Table Styling */
    .table {
      background: rgba(240, 246, 255, 0.85);
      border-radius: 10px;
      overflow: hidden;
    }

    .table th {
      background: #cfe4ff; /* Softer blue */
      color: white;
      padding: 15px;
      font-size: 18px;
      text-align: center;
    }

    .table td {
      padding: 12px;
      vertical-align: middle;
      text-align: center;
    }

    .table-hover tbody tr:hover {
      background: #d0eaff !important; /* Slightly darker blue */
    }

    /* Profile Images */
    .table img {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid #0b3d91;
    }
  </style>
</head>

<body>

  <%- include ('partials/navbar.ejs') %>

  <div class="container">
    <% if (session.user && session.user.userRole !== 'admin') { %>
      <div class="error-box">
        ⛔ Only for Admins
      </div>
    <% } else { %>
      <h2>📜 Users List</h2>

      <table class="table table-hover">
        <thead>
          <tr>
            <th>Image</th>
            <th>Username</th>
            <th>Email</th>
            <th>Role</th>              
          </tr>
        </thead>
        <tbody>
          <% for(let i = 0; i < users.length; i++) { %>
            <tr>
              <td><img src="images/<%= users[i].userImage %>"></td>
              <td><%= users[i].username %></td>
              <td><%= users[i].userEmail %></td>
              <td><%= users[i].userRole %></td>
              <td>
                <form action="/deleteUser/<%= users[i].userId %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this user?');">
                  <button type="submit" class="btn btn-danger">Delete</button>
                </form>
              </td>
            </tr>
          <% } %>
        </tbody>
        
      </table>
    <% } %>
  </div>

</body>
</html>
