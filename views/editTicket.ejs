<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rally AI</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('/images/k.jpg') no-repeat center center fixed;
            background-size: cover;
            color: white;
            margin: 0;
            padding: 0;
        }
        .container {
            background: rgba(255, 255, 255, 0.9);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            margin-top: 50px;
            color: black;
        }
        .btn-submit {
            background-color: #008080;
            color: white;
            padding: 12px;
            border-radius: 5px;
            width: 100%;
            transition: background-color 0.3s;
            border: none;
        }
        .btn-submit:hover {
            background-color: #006666;
        }
    </style>
</head>
<body>

    <%- include ('partials/navbar.ejs') %>

    <div class="container">
        <h2 class="text-center">Update Ticket</h2>
        <form action="/editTicket/<%= ticket.ticketId %>" method="POST" enctype="multipart/form-data">
            
            <!-- Event Name -->
            <div class="form-group">
                <label for="eventName">Event Name:</label>
                <input type="text" id="eventName" name="eventName" class="form-control" value="<%= ticket.eventName %>" required>
            </div>

            <!-- Event Description -->
            <div class="form-group">
                <label for="eventDescription">Event Description:</label>
                <textarea id="eventDescription" name="eventDescription" class="form-control" required><%= ticket.eventDescription %></textarea>
            </div>

            <!-- Ticket Price -->
            <div class="form-group">
                <label for="ticketPrice">Ticket Price:</label>
                <input type="number" step="0.01" min="0" id="ticketPrice" name="ticketPrice" class="form-control" value="<%= ticket.ticketPrice %>" required>
            </div>

            <!-- Available Tickets -->
            <div class="form-group">
                <label for="availableTickets">Available Tickets:</label>
                <input type="number" id="availableTickets" name="availableTickets" class="form-control" value="<%= ticket.availableTickets %>" required>
            </div>

            <!-- Current Image -->
            <div class="form-group">
                <label for="image">Current Image:</label><br>
                <input type="hidden" name="currentImage" value="<%= ticket.eventImage %>">
                <img src="/images/<%= ticket.eventImage %>" width="20%" class="mb-3">
            </div>

            <!-- Upload New Image -->
            <div class="form-group">
                <label for="eventImage">New Image (Optional):</label>
                <input type="file" id="eventImage" name="eventImage" class="form-control" accept="image/*">
            </div>

            <!-- Submit Button -->
            <button type="submit" class="btn btn-submit mt-3">Update Ticket</button>
        </form>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
