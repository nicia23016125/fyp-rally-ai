<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rally AI</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: Arial, sans-serif;
            background: url('/images/k.jpg') no-repeat center center fixed;
            background-size: cover;
        }

        .navbar {
            background-color: rgba(223, 166, 241, 0.7);
        }

        .navbar-brand img {
            height: 50px;
        }

        .navbar-nav .nav-link {
            color: white !important;
            transition: color 0.3s;
        }

        .navbar-nav .nav-link:hover {
            color: grey !important;
        }

        main {
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .content {
            display: flex;
            flex: 1;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .right-side {
            width: 100%; /* Changed to fill the max-width container */
            max-width: 500px; /* Limits width to keep it looking like a box */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: rgba(255, 255, 255, 0.9);
            padding: 40px;
            color: #333;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .right-side h4 {
            color: #ad6ee8;
            margin-bottom: 20px;
            font-size: 2em;
            text-align: center;
        }

        .register-form {
            width: 100%;
        }

        .register-form .section {
            display: flex;
            flex-direction: column;
            margin-bottom: 20px;
            width: 100%;
        }

        .register-form .section label {
            font-weight: bold;
            margin-bottom: 10px;
        }

        .register-form .section input,
        .register-form .section select {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 16px;
        }

        .register-form .section button {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            border: none;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            background-color: #ad6ee8;
            color: white;
        }

        .register-form .section button:hover {
            background-color: #9a5dc5;
        }
    </style>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <main>
        <div class="content">
            <div class="right-side">
                <h4>Register</h4>

                <% if (messages && messages.length > 0) { %>
                    <div class="alert alert-danger" style="width: 100%;">
                        <% messages.forEach(function(message) { %>
                            <p style="margin: 0;"><%= message %></p>
                        <% }); %>
                    </div>
                <% } %>

                <form action="/register" method="POST" enctype="multipart/form-data" class="register-form">
                    <div class="section">
                        <label for="userName">Username:</label>
                        <input type="text" id="userName" name="userName" required value="<%= formData ? formData.userName : '' %>">
                    </div>
                    
                    <div class="section">
                        <label for="userEmail">Email:</label>
                        <input type="email" id="userEmail" name="userEmail" required value="<%= formData ? formData.userEmail : '' %>">
                    </div>
                    
                    <div class="section">
                        <label for="userPassword">Password:</label>
                        <input type="password" id="userPassword" name="userPassword" required>
                    </div>
                    
                    <div class="section">
                        <label for="userImage">Profile Picture:</label>
                        <input type="file" id="userImage" name="userImage" accept="image/*" required>
                    </div>
                    
                    <div class="section">
                        <label for="userRole">Role:</label>
                        <select id="userRole" name="userRole" required>
                            <option value="user" <%= formData && formData.userRole === 'user' ? 'selected' : '' %>>User</option>
                            <option value="admin" <%= formData && formData.userRole === 'admin' ? 'selected' : '' %>>Admin</option>
                        </select>
                    </div>
                    
                    <div class="section">
                        <button type="submit">Register</button>
                    </div>

                    <p style="text-align: center; font-size: 0.9em; margin-top: 10px;">
                        Already have an account? <a href="/login" style="color: #ad6ee8; font-weight: bold;">Log in here</a>
                    </p>
                </form>
            </div>
        </div>
    </main>
</body>
</html>