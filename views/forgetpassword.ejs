<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rally AI</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: Arial, sans-serif;
            background: url('/images/abc.jpg') no-repeat center center fixed;
            background-size: cover;
        }
        .navbar {
            background-color: rgba(223, 166, 241, 0.7); 
        }
        .navbar-brand img {
            height: 50px;
        }
        .navbar-nav .nav-link {
            color: white !important;
            transition: color 0.3s;
        }
        .navbar-nav .nav-link:hover {
            color: grey !important; 
        }
        main {
            flex: 1;
            display: flex;
            flex-direction: column;
        }
        .content {
            display: flex;
            flex-direction: row;
            flex: 1;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .left-side {
            position: relative;
            width: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
            padding: 40px;
        }
        .left-side::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('/images/xxx.png') no-repeat center center;
            background-size: cover;
            opacity: 0.7;
            z-index: 1;
            border-radius: 10px; 
        }
        .left-side > div {
            position: relative;
            z-index: 2;
        }
        .left-side h2 {
            font-size: 3em;
            font-weight: bold;
            margin-bottom: 0.5em;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5); 
        }
        .right-side {
            width: 50%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: rgba(255, 255, 255, 0.9);
            padding: 40px;
            color: #333;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        .right-side h4 {
            color: #ad6ee8;
            margin-bottom: 10px;
            font-size: 2em;
            text-align: center;
        }
        .right-side p {
            color: #ad6ee8;
            font-size: 1.2em;
            text-align: center;
            margin-bottom: 40px;
        }
        .pin-container {
            display: flex;
            justify-content: center;
            gap: 10px;
        }
        .pin-input {
            width: 50px;
            height: 50px;
            font-size: 24px;
            text-align: center;
            border: 2px solid #ad6ee8;
            border-radius: 10px;
            outline: none;
            transition: border-color 0.3s;
        }
        .pin-input:focus {
            border-color: #6b3fa0;
        }
        .reset-form .section button {
            width: 100%;
            padding: 10px;
            margin: 20px 0;
            border-radius: 5px;
            border: none;
            font-size: 16px;
            cursor: pointer;
            background-color: #ad6ee8;
            color: white;
        }
    </style>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <main>
        <div class="content">
            <div class="left-side">
                <div>
                    <h2>Enter Your PIN</h2>
                </div>
            </div>
            <div class="right-side">
                <h4>Forgot Password</h4>
                <p>Enter your registered email and 6-digit PIN to reset your password</p>

                <% if (error.length > 0) { %>
                    <div class="alert alert-danger"><%= error %></div>
                <% } %>
                <% if (success.length > 0) { %>
                    <div class="alert alert-success"><%= success %></div>
                <% } %>

                <form action="/forgetPassword" method="POST" class="reset-form">
                    <div class="email-container">
                        <input type="email" id="userEmail" name="userEmail" class="email-input" placeholder="Enter your email" required>
                    </div>

                    <div class="pin-container">
                        <input type="text" class="pin-input" maxlength="1" required>
                        <input type="text" class="pin-input" maxlength="1" required>
                        <input type="text" class="pin-input" maxlength="1" required>
                        <input type="text" class="pin-input" maxlength="1" required>
                        <input type="text" class="pin-input" maxlength="1" required>
                        <input type="text" class="pin-input" maxlength="1" required>
                    </div>

                    <input type="hidden" name="pinCode" id="pinCode">
                    <div class="section">
                        <button type="submit">Submit</button>
                    </div>
                </form>

                <p><a href="/login">Back to Login</a></p>
            </div>
        </div>
    </main>

    <script>
        const pinInputs = document.querySelectorAll(".pin-input");
        const pinCodeField = document.getElementById("pinCode");

        pinInputs.forEach((input, index) => {
            input.addEventListener("input", (e) => {
                if (e.target.value.length === 1 && index < pinInputs.length - 1) {
                    pinInputs[index + 1].focus();
                }
                updateHiddenPin();
            });

            input.addEventListener("keydown", (e) => {
                if (e.key === "Backspace" && index > 0 && !e.target.value) {
                    pinInputs[index - 1].focus();
                }
            });
        });

        function updateHiddenPin() {
            let pin = "";
            pinInputs.forEach((input) => {
                pin += input.value;
            });
            pinCodeField.value = pin;
        }
    </script>
</body>
</html>
