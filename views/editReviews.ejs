<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Rally AI</title>
    </head>
<body>

    <%- include ('partials/navbar.ejs') %>
    <div class="container">
        <br>
        <div class="text-center">
            <h2>Edit Review for <%=review.productName%></h2>
            <p><%=review.productDescription%></p>
            <p><img src = "/images/<%= review.productImage %>" width="50%"></p>
            <p>Price: $<%=review.productPrice%></p>
        </div>
        <br>
        <div class="text-center">
            <form action="/editReview/<%=review.reviewId%>" method="POST" enctype="multipart/form-data">
                <label for="reviewContent">Review:</label><br>
                <textarea id="reviewContent" name="reviewContent" required><%=review.reviewContent%></textarea> <br>
                <label for="reviewRating">Rating:</label><br>
                <select name="reviewRating" id="reviewRating" required >
                  <% for (let i = 1; i <= 5; i++) { %>
                    <option value="<%= i %>" <%= review.reviewRating === i ? 'selected' : '' %>><%= i %> star</option>
                  <% } %>
                </select>
                <br><br>
                <label for="currentReviewImage">Current Review Image:</label><br>
                <input type="hidden" name="currentReviewImage" value = <%= review.reviewImage %> ><br>
                <img src = "/images/<%= review.reviewImage %>" width="20%"><br><br>

                <label for="reviewImage">Image:</label><br>
                <input type="file" id="reviewImage" name="reviewImage" accept="image/*" ><br><br>

                <label for="reviewedByUserId">Review By:</label><br>
                <input type="text" id="reviewedByUserId" name="reviewedByUserId" value="<%=review.reviewedByUserId%>" required /> <br>
                <button type="submit">Update Review</button>
            </form>
        </div>
    </div>
</body>
</html>
