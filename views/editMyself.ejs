<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Rally AI</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('/images/k.jpg') no-repeat center center fixed;
            background-size: cover;
            color: white;
        }

        .edit-profile-container {
            max-width: 500px;
            background-color: rgba(255, 255, 255, 0.9);
            color: black;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
        }

        .profile-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #e6eefb;
        }

        input[type="text"], input[type="file"] {
            width: 100%;
            padding: 8px;
            margin: 5px 0;
            border-radius: 5px;
            border: 1px solid #ccc;
        }

        .update-btn {
            background-color: #28a745;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
            display: block;
            width: 100%;
            text-align: center;
            transition: 0.3s;
            border: none;
        }

        .update-btn:hover {
            background-color: #218838;
            color: white;
        }
    </style>
</head>

<body>

    <%- include ('partials/navbar.ejs') %>

    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="edit-profile-container text-center">
            <h2 class="mb-4">Edit Profile</h2>

            <form action="/editMyself" method="POST" enctype="multipart/form-data">
                <label for="userName"><strong>Username:</strong></label>
                <input type="text" id="userName" name="userName" value="<%=user.username%>" required>

                <label for="userImage"><strong>Current Image:</strong></label>
                <br>
                <img src="/images/<%= user.userImage %>" class="profile-image mb-3">
                <input type="hidden" name="currentImage" value="<%= user.userImage %>">

                <label for="userImage"><strong>Upload New Image:</strong></label>
                <input type="file" id="userImage" name="userImage" accept="image/*">

                <br> <br>
                <button type="submit" class="update-btn">Update Profile</button>
            </form>
            <form action="/deleteMyself" method="POST" onsubmit="return confirm('Are you sure you want to delete your account? This action cannot be undone!');">
                <button type="submit" class="btn btn-danger w-100 mt-3">Delete Profile</button>
            </form>
        </div>
    </div>

</body>
</html>
