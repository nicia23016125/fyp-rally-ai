<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dedicated Campus Assistant - Uni Helper Pro</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/@n8n/chat/dist/style.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        /* --- 1. GLOBAL & BACKGROUND STYLES --- */
        body { 
            font-family: 'Inter', sans-serif; 
            background: url('/images/k.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #1E293B; 
            min-height: 100vh;
            margin: 0;
            display: flex;
            flex-direction: column;
        }

        /* --- 2. DEDICATED CHATBOT POSITIONING --- */
        :root {
            --n8n-chat-primary-color: #2563EB;
            --n8n-chat-button-background: #2563EB;
            /* Positioning the toggle button and window to the right */
            --n8n-chat-button-right: 30px;
            --n8n-chat-button-bottom: 30px;
            --n8n-chat-widget-window-right: 30px;
            --n8n-chat-widget-window-bottom: 100px;
        }

        /* --- 3. PAGE CONTENT LAYOUT --- */
        .main-content {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: flex-start; /* Keeps text on the left */
            padding-left: 5%;
            padding-right: 450px; /* Space reserved for the chatbot on the right */
        }

        .hero-text-box {
            background: rgba(255, 255, 255, 0.9);
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
            max-width: 600px;
        }

        .hero-text-box h1 {
            font-size: 3rem;
            font-weight: 800;
            color: #0F172A;
            margin-bottom: 20px;
        }

        .hero-text-box p {
            font-size: 1.2rem;
            color: #475569;
            margin-bottom: 25px;
        }

        /* Responsive handling for mobile */
        @media (max-width: 992px) {
            .main-content {
                padding: 20px;
                justify-content: center;
            }
            .hero-text-box {
                text-align: center;
            }
        }
    </style>
</head>
<body>

    <%- include ('partials/navbar.ejs') %>

    <main class="main-content">
        <div class="hero-text-box">
            <h1><i class="fas fa-graduation-cap text-primary me-3"></i>Upload to Tiktok</h1>
            <p>
                Get expert guidance on entering Tiktok with AI-powered suggestions for video optimization,
                professional captioning, and trend analysis to maximize your reach and engagement.
            </p>
            <div class="alert alert-info border-0 shadow-sm">
                <i class="fas fa-arrow-right me-2"></i> 
                Our **Tiktok Assistant** is ready to chat on the right!
            </div>
        </div>
    </main>

    <div id="n8n-chat"></div>

    <script type="module">
    import { createChat } from 'https://cdn.jsdelivr.net/npm/@n8n/chat/dist/chat.bundle.es.js';

    createChat({
        webhookUrl: "https://n8ngc.codeblazar.org/webhook/4091fa09-fb9a-4039-9411-7104d213f601/chat",
        mode: 'window', 
        showWelcomeScreen: true,
        
        // --- 1. SEND DATA TO N8N BACKEND ---
        metadata: {
            video_id: "<%= typeof videoId !== 'undefined' ? videoId : '' %>",
            original_prompt: "<%= typeof prompt !== 'undefined' ? prompt : '' %>",
            user_id: "<%= session.user ? session.user.userId : '' %>"
        },

        // --- 2. DISPLAY PROMPT IN THE CHAT WINDOW ---
        initialMessages: [
            '👋 Hi! I see you want to optimize this prompt:',
            'How would you like to refine it for Tiktok trends?'
        ],
        
        i18n: {
            en: {
                title: 'Tiktok Optimizer',
                subtitle: 'AI Content Assistant',
                footer: 'Powered by n8n AI',
                getStarted: 'Start optimizing now!',
                inputPlaceholder: 'Type your request here...'
            }
        }
    });
     </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>