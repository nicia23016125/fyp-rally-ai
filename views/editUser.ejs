<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Rally AI</title>

    <style>
        /* Page Styling */
        body {
            background-color: #e8f4ff; /* Light pastel blue */
            font-family: 'Arial', sans-serif;
        }

        .container {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            margin-top: 40px;
            max-width: 600px;
        }

        h2 {
            color: #0b3d91; /* Dark blue */
            font-weight: bold;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Form Styling */
        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            font-weight: bold;
            color: #0b3d91;
            margin-top: 10px;
        }

        input, select {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border: 2px solid #cfe4ff;
            border-radius: 8px;
            outline: none;
            transition: 0.3s;
        }

        input:focus, select:focus {
            border-color: #0b3d91;
            box-shadow: 0 0 5px rgba(11, 61, 145, 0.12);
        }

        /* Image Preview */
        .user-image {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #0b3d91;
            margin-top: 10px;
        }

        /* Button Styling */
        .btn-update {
            background: #20c997; /* Mint green */
            color: white;
            font-size: 16px;
            padding: 10px 20px;
            border-radius: 8px;
            border: none;
            cursor: pointer;
            transition: 0.3s ease-in-out;
            margin-top: 20px;
            width: 100%;
        }

        .btn-update:hover {
            background: #17a589;
        }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }

            .btn-update {
                font-size: 14px;
                padding: 8px 15px;
            }
        }
    </style>
</head>
<body>

    <%- include ('partials/navbar.ejs') %>

    <div class="container">
        <h2>📝 Edit User</h2>

        <form action="/editUser/<%=user.userId%>" method="POST" enctype="multipart/form-data">
            <label for="userName">User Name:</label>
            <input type="text" id="userName" name="userName" value="<%=user.username%>" required>

            <!-- Current Profile Image -->
            <label for="userImage">Current Image:</label>
            <input type="hidden" name="currentImage" value="<%= user.userImage %>">
            <img src="/images/<%= user.userImage %>" class="user-image">

            <!-- Upload New Image -->
            <label for="userImage">New Image:</label>
            <input type="file" id="userImage" name="userImage" accept="image/*">

            <!-- Role Selection -->
            <label for="userRole">User Role:</label>
            <select id="userRole" name="userRole" required>
                <option value="user" <% if (user.userRole === 'user') { %> selected <% } %>>User</option>
                <option value="admin" <% if (user.userRole === 'admin') { %> selected <% } %>>Admin</option>
            </select>

            <!-- Update Button -->
            <button type="submit" class="btn-update">Update User</button>
        </form>
    </div>

</body>
</html>
